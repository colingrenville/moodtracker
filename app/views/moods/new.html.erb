<p id="newtitle">New Day</p>

<div class="yourmood">
<%= form_for(@mood) do |f| %>
	<% if @mood.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@mood.errors.count, "error") %> prohibited your shit from being saved, dude:</h2>
			<ul>
			<% @mood.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<%end%>

	<div class="field">
		<%= f.label :rating %>
		<%= f.text_field :rating %>
	</div>

	<div class="field">
		<%= f.label :entry %>
		<%= f.text_field :entry %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
	
<%end%>
</div>

